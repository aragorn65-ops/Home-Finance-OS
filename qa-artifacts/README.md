# QA Artifacts

This directory stores sprint QA evidence that is useful to keep with the repo,
such as screenshots and short run logs.

Browser profile directories and generated cache state are intentionally ignored
because they are large, machine-specific, and may contain local session data.
